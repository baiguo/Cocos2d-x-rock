let CC_SIMULATOR = true;//is simulator
let CC_SIMULATOR_RECORD_MODE = false; // is record mode in simulator (for android instant)
let CC_SIMULATOR_RECORD_PATH = "";// record save path (for android instant game)
